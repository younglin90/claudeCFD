# 5-equation IMEX production path — C++ port spec (Phase A)

Foundation-up port plan for the **production** all-Mach 5-equation IMEX step.
Target = a header-only C++ twin of the Python stage map `imex_ad_step`, validated
bit-comparably (rel ≤ 1e-12) against the oracle files in `cpp/tests/5eq_ref/`
(generated by `cpp/tools/gen_5eq_oracle.py`).

Python source root: `solver_5eq/solver/five_eq_IMEX/` (FROZEN oracle — never edit).
C++ target subdir: `cpp/include/cfd/five_eq/` (NEW — create in Phase B).

## Port status (2026-07-15)

**The Python `solver_5eq/solver/five_eq_IMEX` solver surface is ported to
C++.** The checked C++ surface includes the validated 1-D production path and
the public Python-compatible driver/options:

- `sound_speed` (Kapila/Wood and frozen-alpha mixture choices), reconstruction,
  SLAU2, adaptive-BVD material update, implicit acoustic stage, `regime_auto`,
  source terms, and 1-D run loop.  The frozen acoustic/material composition
  has a separate 40-cell Python oracle.
- `imex_ad_ssp2`; all Python 1-D legacy `strang`, `split`, `be1`, and `be_full`
  selectors; both Shu-Osher SSP3 scopes and Pareschi-Russo stage-residual SSP3
  (material or explicit-residual operator, stage/final PE relax); pressure-based
  first-order `explicit_rusanov_step`; primitive inlet/outlet and acoustic-inlet
  boundary data; pressure/pT relaxation APIs.
- Scalar `PrimND`/`ConsND` transforms, periodic prescribed-velocity 2-D alpha
  transport, and a Cartesian 2-D/3-D HLLC + SSPRK3 field solver.  The latter
  exposes `solve_nd` / `solve_2d` / `solve_3d` as well as periodic
  convenience wrappers, uses
  row-major-consistent neighbour indexing, Superbee/minmod/van-Leer/first-order
  primitive reconstruction, periodic/transmissive/reflective boundaries, and
  optional gravity and Python's `mixture_c` selection (Kapila/frozen).  Python
  one-step oracles cover first-order 2-D (`5.5e-12`),
  2-D TMLPU/mstacs (`4.0e-12`), mixed 2-D boundaries (`2.7e-12`), 2-D gravity
  (`5.3e-12`), and 3-D TMLPU/mstacs (`3.7e-12`).
- ARS222 stage accumulation, implicit residual faces, ACID/APEC explicit
  residual, and a dense-FD Newton reference solver.  Its small mixed-state
  Python oracle currently agrees to `7.1e-6` relative; it is not yet a
  production large-grid Newton replacement.  The periodic Helmholtz assembly
  and cyclic-tridiagonal solve are ported independently (`8.7e-19` oracle
  error).  Periodic Schur block elimination, one Helmholtz correction, damped
  residual-decreasing iterations, and the `ARSLinearSolver::schur_helmholtz`
  ARS222 selection are now ported.  The one-correction Python oracle is
  `4.2e-8`; a mixed ARS222 case converges in both stages and differs from the
  dense path by `1.1e-12`.  Dense Newton remains the default.
- Periodic Rhie-Chow implicit faces are ported and selectable for dense ARS
  stages (`StepConfig::ars_rhie_chow`).  The face/divergence oracle agrees to
  `1.9e-16`; the full mixed ARS222 Rhie-Chow oracle agrees to `7.1e-6`.
- The remaining Python implicit-face selections are ported for dense ARS:
  all-face `acoustic_riemann` (face oracle `3.5e-18`, full ARS oracle
  `7.1e-6`) and `upwind` bias (face oracle `3.5e-18`).  Schur deliberately
  falls back to dense Newton for these non-central face forms.
- The ARS explicit residual now has Python-matched dt-gated layered
  positivity blending, PE-preserving and diagnostic Rusanov low fluxes, and
  `force_lo` semantics.  Its blended, no-dt, and dt-enabled force-low oracle
  paths agree within `2e-12`.
- `FaceStateOptions` exposes the Python 1-D face-state choices used by ARS:
  upwind/MUSCL/central/CICSAM/MSTACS/STACS/vanLeer/THINC/THINC-BVD/
  adaptive-BVD alpha, central/upwind `u,p`, upwind/central/TVD/WENO3
  thermodynamic primitives, and ACID/cell thermodynamics.  A nine-case
  high-order oracle agrees to `1.4e-15`.  ARS injects its stage `dt/dx` into
  these options.
- PE energy-only correction and full tangent projection are ported.  In the
  NASG oracle, corrected energy and tangent residual agree to `2.9e-12` and
  `2.6e-17`; raw `dpdU` differs by `2.3e-7` because the mixed-scale 5x5
  inverse is ill-conditioned.  Face/update PE diagnostics are also ported;
  the non-equilibrium cancellation oracle agrees to `2.4e-8` relative.
- PE tangent projection now includes Python's `always`, `contact`, `interface`,
  `interface_band`, `impedance`, and smooth `sensor` modes.  The six-mode
  periodic material-interface oracle agrees to `4.9e-12`.  BE1 exposes these
  modes, their `*_explicit` composition, and the legacy energy-only correction.
- The periodic uniform-u/uniform-p finite-volume remap, `dt_min`, time-varying
  inlet/outlet callables, and step callback termination are represented by
  `RunConfig`.  Acoustic `interface_be` and pure-tolerance-consistent
  coefficients are `StepConfig` opt-ins.  The acoustic selector also covers
  ACID, TR-BDF2, WAF (`nu`, `one_minus_nu`, sensor), dissipation consistency,
  first-order/stencil-clean/one-sided-interface reconstruction, and
  component/characteristic/WENO3/MUSCL3/BVD/WENO5 reconstruction.
- `imex_ad` now covers all normalized pressure-closure branches, including
  `implicit_energy`, `implicit_energy_momentum`, and `apec_pe`; `apec_pe`
  selects the Python secant APEC material-energy flux.  The material update
  also exposes the no-Kapila, path, cell, hybrid, trapezoid,
  immiscible-trapezoid, mixed-trapezoid, and mixed-path alpha sources.
- The 1-D source driver uses the public Python source-term composition order
  for gravity, Lee phase change, and heat conduction, including the
  primitive-temperature/isothermal precedence rules.  Its mixed-source oracle
  is bounded by `1e-6`, reflecting the independently stopped scalar versus
  vectorized 3x3 primitive-recovery Newton iterations.
- `cons_to_prim_W` now ports Python's fixed-density near-pure fallback; its
  four-cell Python oracle agrees within `9.0e-13`.  BE1 also exposes
  `kapila_acoustic_source`, `implicit_include_explicit_residual`, and
  `newton_kwargs` equivalents (`newton_max_iter`, `newton_rtol`,
  `newton_atol`, `newton_line_search_max`, `newton_eta`), plus conservative final-update backtracking with the same
  finite-state fallback policy.  BE1 maps Python `energy_form='apec'` to the
  C++ secant APEC energy flux; ARS keeps its differential default.
- `FaceStateOptions::energy_alpha_pure_tol` now separately controls the
  APEC pure-face collapse tolerance, matching Python's
  `energy_alpha_pure_tol`; it is propagated through explicit residual,
  material SSP3, and BE1 paths.  `eos_facade.py::make_eos`/`EOSPair` and
  `primitive.py::pack_W`/`unpack_W`/`uniform_W` have typed C++ facade
  counterparts.  ND exposes matching density and conservative-alpha clip
  helpers.
- `RunConfig::python_solve_defaults()` and `solve(...)` expose the direct C++
  counterpart to `main.py::solve` without numerical kwargs. Its one-step
  Python oracle agrees within `4.8e-10`.
- Fresh gate: `ctest` has 82 tests, including Python-oracle 02-A/07-B,
  explicit inlet/Dirichlet, dense and Schur ARS, PE correction, and 2-D/3-D
  field checks.  C++ acceptance apps pass 02-A and all Air-Water,
  Helium-Air, and Argon-Air 07-B cases.

There are no remaining public Python solver paths absent from the C++ port.
Future work is performance-oriented: a dedicated non-periodic sparse
Schur/Helmholtz preconditioner and a production large-grid implicit ND pressure
block. Python's ND CICSAM/STACS/MSTACS/THINC names currently select the same
bounded Superbee alpha limiter, represented by `NDReconstruction::alpha_superbee`.

> **Production path resolved from the code (not assumptions):**
> The acceptance harness `.codex-loop/verify_02_07_acceptance.py` calls
> `main.solve(...)` with `time_integrator = os.environ.get("FIVE_EQ_IMEX_TIME_INTEGRATOR", "imex_ad")`.
> **The default integrator is `imex_ad` (single-stage), not `imex_ssp3`.** Both
> route through the SAME stage map `imex_ad_step` (`imex_ad.py:3902`). The
> `imex_ssp3`/`FIVE_EQ_IMEX_SSP3_FORM=shu_osher` path
> (`time_integrator.py:872 imex_ad_ssp3_step`, default
> `FIVE_EQ_IMEX_SSP3_SCOPE=transport_acoustic_cn`) is a thin **conservative
> 3-fold Shu-Osher composition of `imex_ad_step`** (`time_integrator.py:928-938`).
> **Therefore the C++ production step = a faithful port of `imex_ad_step`; the
> SSP3 wrapper is Module 6/10 layered on top.** Port `imex_ad_step` first.

---

## C++ conventions (follow `cpp/MIGRATION.md` + existing `eos.hpp`/`primitive.hpp`)

- **Header-only**, POD structs + enum tags, no virtual dispatch on hot paths.
- Every leaf math routine is a scalar function marked `CFD_ROUTINE_SEQ`
  (`acc routine seq` on device / no-op on host); array work lives in the
  caller's OpenMP/OpenACC loop. Per-cell / per-face scalar funcs + caller loop.
- **Bit-comparable clamps**: reproduce every Python guard EXACTLY.
  `np.maximum(x, 1e-30)` → `EOS::max2(x, 1e-30)`; `_EPS = 1e-30` throughout the
  5eq Python. Record each constant (below) and clamp identically.
- Reuse `cfd::EOS` (`eos.hpp`) and `cfd::PrimW`/`ConsU` + `cons_to_prim_W`
  (`primitive.hpp`). **Confirmed present** — see EOS/primitive reuse note.
- Each module gets a `test_5eq_<name>.cpp` asserting rel ≤ 1e-12 vs the matching
  `cpp/tests/5eq_ref/*.txt` file (same `check()` pattern as `test_eos.cpp`).
- Types: `double` only; `bool` masks → `std::vector<char>` or bitset in caller.
- The implicit acoustic linear solve is the ONE place needing a banded solver
  (Thomas / LAPACK `dgbsv`); everything else is stencil arithmetic.

---

## Module port table (foundation-up)

| # | Module | Python source (file::func lines) | C++ target header | Deps | Validation ref |
|---|--------|----------------------------------|-------------------|------|----------------|
| 0 | EOS (Ideal/SG/NASG + p,T derivs) | `He2024/eos_general.py` | `cfd/eos.hpp` ✅ done | — | `test_eos` (done) |
| 0 | Prim↔cons + Newton recovery | `He2024/primitive_W.py`, `primitive.py` near-pure fallback | `cfd/primitive.hpp` ✅ done | eos | `test_primitive`, `test_5eq_primitive_near_pure` |
| 1 | Mixture c² (Wood/Kapila or frozen) + phase Z, pure-branch | `sound_speed.py::phase_sound_speed_sq` 21-33, `mixture_sound_speed_sq` 36-48; `explicit.py::_phase_acoustic` 22-40 | `cfd/five_eq/sound_speed.hpp` | eos | `sound_speed_ref.txt`, `acoustic_frozen_ref.txt` |
| 2 | EOS-consistent face energy coeffs (APEC χ_k/χ_α) | `energy_flux.py::_secant_chi` 42-125, `apec_energy_flux` 128-167, `total_energy_flux` 170-204 | `cfd/five_eq/energy_flux.hpp` | eos, m1 | `step_02A/07B_ref.txt` (indirect) |
| 3 | SLAU2 material face velocity | `imex_ad.py::_slau2_faces_np` 599-653 | `cfd/five_eq/slau2.hpp` | m1, recon(m5) | `slau2_faces_ref.txt` |
| 4 | adaptive_bvd α transport (CICSAM/MUSCL-Hancock) | `explicit.py::_adaptive_bvd_alpha_face` 410-456, `_cicsam_alpha_face` 63-114, `_muscl_hancock_alpha_face` 273-319, `_tvd_slope_1d` 246-270 | `cfd/five_eq/alpha_bvd.hpp` | — | `alpha_bvd_ref.txt` |
| 5 | tmlpu primitive reconstruction (+ TVD, LMP bound, Hancock) | `reconstruction.py::_tvd_limiter` 70-89, `_limited_value` 92-129, `reconstruct_upwind_faces` 160-197, `reconstruct_lr_faces` 200-232 | `cfd/five_eq/reconstruct.hpp` | — | `weno5`? no — covered via `slau2`/`step` refs |
| 6 | Material update (SSP3 Shu-Osher composition) | `imex_ad.py::_material_update` 2170-2733; `time_integrator.py::imex_ad_ssp3_step` 872-944 | `cfd/five_eq/material_update.hpp`, `cfd/five_eq/ssp3.hpp` | m1-m5, prim | `step_02A/07B_ref.txt` |
| 7 | Implicit acoustic solve (Wood-Z Riemann + WENO5 + θ-method + banded) | `imex_ad.py::_solve_acoustic_ad` 2736-3681, `_acoustic_faces_np` 464-512, `_weno5_face_left_np` 1396-1418, `_weno3_face_left_np` 1373-1393 | `cfd/five_eq/acoustic_solve.hpp`, `cfd/five_eq/weno.hpp` | m1, m5, banded solver | `acoustic_solve_ref.txt`, `weno5_face_ref.txt` |
| 8 | regime_auto energy-closure automaton | `imex_ad.py::imex_ad_step` 3912-3927 + closures 4024-4061; `_pressure_jump_high_to_low_impedance` 230-236, `_compressive_pressure_mask` 93-114 | `cfd/five_eq/regime_auto.hpp` | m1, m6, m7 | `step_02A/07B_ref.txt` |
| 9 | Newton primitive recovery (3×3 on p,T1,T2) | `primitive.py`/`He2024` → already `primitive.hpp::cons_to_prim_W` ✅ | `cfd/primitive.hpp` (reuse) | eos | `test_primitive` (done) |
| 10 | Step driver + fixed BASE_ENV config | `imex_ad.py::imex_ad_step` 3902-4090; `main.py::solve` 330-509 | `cfd/five_eq/step.hpp`, `cfd/five_eq/config.hpp` | m1-m9 | `step_02A/07B_ref.txt` |

BASE_ENV (production defaults, fixed as C++ compile-time config — Module 10):
`time_integrator=imex_ad`, `alpha_scheme=adaptive_bvd`, `primitive_scheme=tmlpu`
(TVD limiter `FIVE_EQ_IMEX_TMLPU_TVD` default `vanleer`; density recon
`FIVE_EQ_IMEX_DENSITY_TVD=minmod`), `material_flux=slau2`,
`pressure_closure=regime_auto`, acoustic recon `weno5` (production default in
C++), `mixture_kind=kapila`, `kapila_closure=True`, `pure_branch=True`,
`FIVE_EQ_IMEX_ACOUSTIC_THETA=0.5`, `kapila_source_mode=mixed_path`.

---

## Module details

### M1 — Mixture sound speed + phase acoustic impedance Z
**`sound_speed.py:21-48`, `explicit.py:22-40`**. `_EPS=1e-30`.

Per-phase c² from analytic (p,T) derivatives (`phase_sound_speed_sq`):
```
pr2   = p / max(rho^2, 1e-30)
Θ_k   = (pr2*drhodp_T - dedp_T) / ( (e_T - pr2*drhodT_p) if |den|>1e-30 else 1e-30 )   # den = dedT_p - pr2*drhodT_p
K_k   = drhodp_T + drhodT_p*Θ_k
c_k^2 = 1 / max(K_k, 1e-30)
```
Kapila mixture (production `kind='kapila'`, `mixture_sound_speed_sq`):
```
rho          = alpha1*rho1 + (1-alpha1)*rho2
inv_rho_c2   = alpha1/max(rho1*c1^2,eps) + (1-alpha1)/max(rho2*c2^2,eps)
c_mix^2      = 1 / max(rho * inv_rho_c2, eps)
```
Production `_phase_acoustic` wraps this and applies the **pure-branch override**
(`explicit.py:32-38`) when `alpha_pure_tol>0`:
```
pure1 = alpha >= 1 - alpha_pure_tol   → rho:=rho1, c_mix^2:=c1^2
pure2 = alpha <= alpha_pure_tol       → rho:=rho2, c_mix^2:=c2^2
Z     = max( rho * sqrt(max(c_mix^2, eps)), eps )
```
rho1,rho2 = `max(eos.density(p,T), eps)`. C++: per-cell scalar `phase_acoustic()`
returning `{rho, c_mix_sq, Z}`; caller loops over cells + ghosts.
**Ref:** `sound_speed_ref.txt` (13 α incl. pure 1e-8 / 1-1e-8 / 0.5; air+NASG-water;
columns give rho1,rho2,c1²,c2²,rho,c_mix²,Z and the raw-vs-override `c_mix_raw`).

### M2 — EOS-consistent face energy coefficients (APEC / Allaire)
**`energy_flux.py:42-204`**. Production `_material_update` uses
`material_energy_form='allaire'` for the `regime_auto→pressure_work_consistent`
/`compressive_recovery` closures (the 02A/07B path): the energy face flux is
simply `F_rE_adv = rE_f * u_star` (`imex_ad.py:2648`) and the APEC secant χ is
NOT on the hot path there. **Port `total_energy_flux`/`apec_energy_flux`/
`_secant_chi` only for the `apec_pe` closure** (mark lower priority). The secant
identity (byte-exact 3-substep path split of `g=q1·e1(q1/α,p)+q2·e2(q2/(1-α),p)`)
is `energy_flux.py:42-125`; differential fallback χ_k = `e_k + rho_k·e_T/rho_T`
at `energy_flux.py:155-158`. Pure-face collapse to single-phase flux
`energy_flux.py:197-202`, `alpha_pure_tol=1e-12`.

### M3 — SLAU2 material face velocity
**`imex_ad.py:599-653`**. Pressure-free all-speed face velocity for the IMEX
split (fills `p_star`/`u_star` where `slau2_valid`, `imex_ad.py:2204-2209`):
```
c_L,c_R   = sqrt(max(c_mix_sq_ext[:-1|1:], eps))            # from M1 on ghost-extended state
c_avg     = max(0.5*(c_L+c_R), eps)
u_rms     = sqrt(0.5*(u_L^2 + u_R^2))
mach_hat  = min(1, u_rms/c_avg)
chi       = (1 - mach_hat)^2                                 # (1-Mhat)^2 coupling
rho_avg   = max(0.5*(rho_L+rho_R), eps)
v_avg     = (sqrt(rho_L)*u_L + sqrt(rho_R)*u_R)/max(sqrt(rho_L)+sqrt(rho_R), eps)  # Roe-avg velocity
u_face    = v_avg - chi*(p_R - p_L)/max(rho_avg*c_avg, eps)
p_face    = 0.5*(p_L + p_R)
valid     = finite(u_face)&finite(p_face)&(rho_L>eps)&(rho_R>eps)
```
L/R states from M5 reconstruction (`reconstruct_lr_faces`, or mixture/
characteristic variants — for `tmlpu` on the pure-interface 02A/07B state the
`_mixture_primitive_recon_enabled`/`_characteristic_recon_allowed` gates decide;
default falls to per-variable `reconstruct_lr_faces` with rho from EOS).
Reflective wall override: `p_face[0]=p_ext[1], u_face[0]=0` (and mirror at right).
**Ref:** `slau2_faces_ref.txt` (8-cell air|water jump + Gaussian bump, tmlpu,
reflective/transmissive; 9 faces).

### M4 — adaptive_bvd α transport
**`explicit.py:410-456`**. Topology-selected (NOT case-id): pick CICSAM
compression on genuine pure-material interfaces, MUSCL-Hancock TVD elsewhere.
Selection (`explicit.py:428-455`), `pure_tol = max(eps^0.25, alpha_pure_tol)`
(`eps^0.25 ≈ 1.2207e-4`), `pure_band = pure_tol*(1+1e-9) + 1e-15`:
```
has_low_pure  = min(interior) <= pure_band
has_high_pure = max(interior) >= 1 - pure_band
has_sharp_pure_jump = any adjacent (a_L<=pure_band & a_R>=1-pure_band) or mirror
has_narrow_mixed_layer = has_low_pure & has_high_pure & (mixed_count <= max(low+high,1))
if has_low_pure & has_high_pure & (has_sharp_pure_jump | has_narrow_mixed_layer):
    → _cicsam_alpha_face(alpha_ext,u_face,dt,dx)
else:
    → _muscl_hancock_alpha_face(alpha_ext,u_face,dt,dx,tvd_kind)
```
- CICSAM (`explicit.py:63-114`): upwind base face; per interior face NVD
  `phi_c=(up-far)/(down-far)`; if `0<=phi_c<=1`: `phi_f=min(1, phi_c/Courant)`,
  `face = far + phi_f*(down-far)`; Courant `clip(|u|dt/dx, 1e-12, 1)`; clip [0,1].
  Periodic-wrap duplicate-face handling `explicit.py:100-113`.
- MUSCL-Hancock (`explicit.py:273-319`): `slope=_tvd_slope_1d(up-far,down-up,kind)`,
  `val = up + 0.5*(1-Courant)*slope`, clamp to local `[min,max](far,up,down)`.
  `_tvd_slope_1d` (`explicit.py:246-270`): minmod/superbee/mc/umist/vanLeer,
  returns 0 if `d_up*d_down<=0`.
**Ref:** `alpha_bvd_ref.txt` (pure-jump → CICSAM branch; smooth sinusoid →
MUSCL-Hancock branch; tvd_kind='umist').

### M5 — tmlpu primitive reconstruction
**`reconstruction.py:70-232`**. 1D bounded T-MLP-u = TVD limiter clipped by a
local 3-cell maximum-principle bound. `_limited_value` (`reconstruction.py:92-129`):
```
r   = (phi_R - phi_L) / (phi_L - phi_LL)          # guard: |num|,|den|<=1e-300 → return phi_L
psi = _tvd_limiter(r, kind)                        # clamped to [0,2]
delta = 0.5*(phi_R - phi_L)
if courant given: delta *= (1 - clip(|courant|,0,1))   # MUSCL-Hancock time-centering
lo,hi = min/max(phi_LL,phi_L,phi_R)
psi_bound = (hi-phi_L)/max(delta,eps)  if delta>0   else (lo-phi_L)/min(delta,-eps) if delta<0 else 0
psi   = max(0, min(2, psi, psi_bound))             # LMP bound
return clip(phi_L + psi*delta, lo, hi)
```
`_tvd_limiter` (`reconstruction.py:70-89`): minmod `min(1,r)`, superbee
`max(min(2r,1),min(r,2),0)`, mc `max(0,min(2r,0.5(1+r),2))`, vanalbada
`(r²+r)/(r²+1)`, umist, default vanLeer `2r/(1+r)`; all `max(0,min(2,psi))`;
returns 0 for `r<=0` or non-finite. `reconstruct_upwind_faces`
(`reconstruction.py:160-197`) picks upwind cell as L via `u_face>=0`, stencil
`(f-1,f,f+1)` or mirror; `tmlpu` alias table `reconstruction.py:30-45`; scheme
`tmlpu` uses `FIVE_EQ_IMEX_TMLPU_TVD` (default `vanleer`). Floors: T1/T2 floor
1.0, p floor 1e-12, u none.
**Ref:** exercised inside `slau2_faces_ref.txt` and `step_*` refs.

### M6 — Material update + SSP3 composition
**`imex_ad.py:2170-2733`** (`_material_update`), **`time_integrator.py:872-944`**
(`imex_ad_ssp3_step`).
Face-velocity base = acoustic Wood-Z Riemann `p_star,u_star`
(`imex_ad.py:2198-2200`, identical formula to M7 `_acoustic_faces_np`), then
overwritten by SLAU2 where valid (`2204-2209`). Upwind conservative faces
`q1_cons=α_up·rho1_f`, `q2_cons=(1-α_up)·rho2_f`, `m_cons=(q1+q2)·u_adv_f`,
`rE_cons=q1·E1_f+q2·E2_f` (`2286-2289`). Sharp-α FCT correction (adaptive_bvd
path, `2290-2498`): single `theta` limiter applied identically to α, phase-mass,
momentum, energy corrections (`delta_alpha=alpha_sharp-alpha_upwind`;
`apply_lmp` face-local + `apply_update_lmp` Zalesak cell-update limiter). Auto
α-TVD: `superbee` if collocated p-material jump else `umist` (`2300-2308`);
FCT auto-enabled when sharp α-interface present and not collocated jump.
Non-conservative α source (kapila_closure, `mixed_path` production mode,
`2657-2708`): Simpson face weight `B_f=(B_ext_L + 4·B_mid + B_ext_R)/6`,
`source_face = (B_f[1:]·(u*[1:]-u_cell) + B_f[:-1]·(u_cell-u*[:-1]))/dx`,
`source_cell = (α + D_K)·div_u`; `mixed_path` uses `source_face` on true-mixture
stencils, hybrid (cell in pure material, face otherwise) elsewhere.
`D_K_kapila` = Kapila closure `source_d1.py`. Conservative update (`2712-2719`):
```
q1_new  = U_n[0] - dt·(F_q1[1:]-F_q1[:-1])/dx
q2_new  = U_n[1] - dt·(F_q2[1:]-F_q2[:-1])/dx
m_adv   = U_n[2] - dt·(F_m_adv[1:]-F_m_adv[:-1])/dx
rhoE    = U_n[3] - dt·(L_rE_adv + L_pu_old)          # L_pu_old = div(p_star·u_star)
alpha_new = U_n[4] - dt·(L_alpha - source_alpha)
rhoE_adv = U_n[3] - dt·L_rE_adv                       # aux, for pressure-work closure
```
SSP3 wrapper (only when integrator=imex_ssp3/shu_osher, `time_integrator.py:928-938`):
`W1=G(Wn)`, `W2=blend(Wn, G(W1), 0.75)`, `Wn+1=blend(Wn, G(W2), 1/3)` where `G`
= one `imex_ad_step`, blends in conservative vars with PE projection on flat
contacts. **Ref:** `step_02A/07B_ref.txt` (full end-to-end 1 step).

### M7 — Implicit acoustic solve  ★ CRITICAL DESIGN NOTE ★
**`imex_ad.py:2736-3681`** (`_solve_acoustic_ad`).
Frozen coefficients at `W^n` (`imex_ad.py:2743-2747`):
```
rho_star = max(q1_new+q2_new, eps)
(rho_anchor, c_mix_sq, Z) = phase_acoustic(W_n)      # M1, with pure override
beta     = max(rho_anchor * c_mix_sq, eps)           # = rho c^2  (Helmholtz stiffness)
```
Face closure = **linear Wood-Z acoustic Riemann** (`_acoustic_faces_np:497-505`,
inline `face_local:2906-2910`):
```
p_f = (Z_r·p_l + Z_l·p_r + Z_l·Z_r·(u_l - u_r)) / (Z_l+Z_r)
u_f = (p_l - p_r + Z_l·u_l + Z_r·u_r) / (Z_l+Z_r)
```
Per-cell θ-method (`imex_ad.py:2855-2899`): `theta_cell = 0.5` (Crank-Nicolson)
on WAVE cells (`p_rel_face > 1e-8`), `1.0` (Backward-Euler) on flat cells;
`FIVE_EQ_IMEX_ACOUSTIC_SCHEME=theta` default. `interface_be` option forces BE in
a stencil-derived 2+1 band across pure|pure material faces (`2870-2899`).
Discrete residual per cell (scalar branch `local_res:3629-3666` — the exact
system; the n≥32 branch is the vectorised twin):
```
div_p = θ_i·(p_fr - p_fl)/dx + (1-θ_i)·div_p_old[i]
div_u = θ_i·(u_fr - u_fl)/dx + (1-θ_i)·div_u_old[i]
dp_dx = upwind(p) by sign(u0[i])                       # (p_c-p_l)/dx or (p_r-p_c)/dx
r_u   = rho_star[i]·u_c - m_adv[i] + dt·div_p          # momentum
r_p   = p_c - p0[i] + dt·(u0[i]·dp_dx + beta[i]·div_u) # pressure: Dp/Dt + rho c² div u = 0
```
`div_*_old` = old-time faces from M5/WENO5 reconstruction of `W^n`
(`_acoustic_faces_muscl_np` for n≥32, `_acoustic_faces_np` for n<32, `2778-2788`).

**HAND-DERIVED ANALYTIC JACOBIAN (replaces torch autodiff).** The Python
assembles J by `torch.func.vmap(jacrev(...))` (the speed bottleneck) with a
**straight-through rule**: the WENO5 face VALUE is nonlinear, but its Jacobian is
frozen to the **linear-optimal 5th-order stencil** (weights 1/10, 6/10, 3/10 —
see the note at `imex_ad.py:1365-1368` and `weno5_left_t:3146-3159`). Because:
1. `face_local` is **linear** in (u,p) with frozen (Z_l, Z_r);
2. `dp_dx` is a linear frozen-sign upwind difference;
3. the WENO5 straight-through derivative IS the linear-optimal WENO5 stencil;
the residual `R(y)` is an **affine function** `R(y) = J·y − b` of `y=(u,p)`.
**⇒ the straight-through torch Jacobian IS the analytic Jacobian of the frozen
linear operator, and ONE Newton step from anchor `y0=(u0,p0)` is exact:**
`y = y0 − J⁻¹·R(y0)` (`_linear_step:3555-3564`, `spsolve`). In C++ this becomes a
directly-assembled banded matrix — no autodiff.

**Banded stencil / bandwidth.** The Jacobian couples, per cell `i`, the columns
(`build_sparse_jacobian_vectorized` column map `imex_ad.py:3542-3552`):
`{i-3,i-2,i-1,i,i+1,i+2,i+3}` in BOTH the u-block and p-block ⇒ for the
`2n×2n` system ordered `[u_0..u_{n-1}, p_0..p_{n-1}]` each of the four `n×n`
sub-blocks (`∂r_u/∂u, ∂r_u/∂p, ∂r_p/∂u, ∂r_p/∂p`) is **7-point (half-bandwidth
3)** when acoustic recon = `weno5`. The i±2, i±3 couplings come ONLY from the
WENO5 5-point face stencil on material-clean faces; on same-pure/flat faces the
Riemann face uses the 3-point (i±1) stencil (half-bandwidth 1, tridiagonal).
The scalar n<32 branch (`3610-3681`) is purely tridiagonal (i±1). C++ Phase B:
assemble the 7-band `2n×2n` operator analytically and solve with LAPACK `dgbsv`
(or a block-tridiagonal solver on the i±1 core with the i±2/i±3 WENO bands).
- `∂r_u/∂u`: `rho_star[i]` on diagonal + `dt·θ·∂(div_p from face pressures)/∂u`
  (via `p_f`'s `Z_l·Z_r·(u_l-u_r)/(Z_l+Z_r)` term) → i±1 (+ WENO i±2,3).
- `∂r_u/∂p`: `dt·θ·∂(div_p)/∂p` (via `p_f` `(Z_r p_l+Z_l p_r)/(Z_l+Z_r)`) → i±1(+WENO).
- `∂r_p/∂u`: `dt·beta[i]·θ·∂(div_u)/∂u` → i±1 (+WENO).
- `∂r_p/∂p`: `1` on diagonal + `dt·(u0·∂dp_dx/∂p + beta·θ·∂(div_u)/∂p)` → i±1(+WENO).
Diagonal Tikhonov floor: `J += 1e-12·max(|diag|,1)·I` before solve (`3559-3563`).
**Ref:** `acoustic_solve_ref.txt` (40-cell interface, `FIVE_EQ_IMEX_ACOUSTIC_RECON=weno5`;
n≥32 so the production WENO5 vectorised path runs), `weno5_face_ref.txt`.

WENO5-JS face value (`_weno5_face_left_np:1396-1418`), right face of q0:
```
c0 = qmm/3 - 7qm/6 + 11q0/6 ; c1 = -qm/6 + 5q0/6 + qp/3 ; c2 = q0/3 + 5qp/6 - qpp/6
b0 = 13/12(qmm-2qm+q0)² + 1/4(qmm-4qm+3q0)²
b1 = 13/12(qm-2q0+qp)²   + 1/4(qm-qp)²
b2 = 13/12(q0-2qp+qpp)²  + 1/4(3q0-4qp+qpp)²
scale = max(b0,b1,b2, 1e-300)                 # _WENO_TINY
a_k = d_k / (1e-6 + b_k/scale)²               # d=(1/10,6/10,3/10), _WENO_EPS_REL=1e-6
value = (a0 c0 + a1 c1 + a2 c2)/(a0+a1+a2)
```
Mirror (left state of cell right of face) = `_weno5_face_left_np(qp3,qp2,qp1,q0,qm1)`.
The straight-through **Jacobian** uses the FROZEN linear weights d=(1/10,6/10,3/10)
directly (a_k → d_k), i.e. the linear 5th-order stencil `[2,-13,47,27,-3]/60` per
side. WENO3-JS twin at `1373-1393` (d=(1/3,2/3)).

### M8 — regime_auto energy-closure automaton
**`imex_ad.py:3912-3927`** (selection) + **`4024-4061`** (closures).
```
pure_tol_auto = max(alpha_pure_tol, eps^0.25)
has_immiscible_interface = (min(α) <= pure_tol_auto) & (max(α) >= 1 - pure_tol_auto)
if has_immiscible_interface:
    if _pressure_jump_high_to_low_impedance(W_n, ...):  → 'compressive_recovery'
    else:                                                → 'pressure_work_consistent'
else:                                                    → 'implicit_energy'
```
Both 02A and 07B have immiscible interfaces → `pressure_work_consistent` (the
default when no high→low impedance p-jump; `_pressure_jump_high_to_low_impedance`
= `_pressure_jump_stiff_to_soft_material` `imex_ad.py:174-236`). Closures:
- `pressure_work_consistent` (`4033-4056`): solve `_solve_acoustic_ad` (M7),
  then rebuild energy from pressure-work faces:
  `rhoE_new = rhoE_adv - dt·(p_f[1:]·u_f[1:] - p_f[:-1]·u_f[:-1])/dx` using
  `_acoustic_faces_np(u_new,p_new,Z)`; pure-shock recovery mask blends
  `_recover_pressure_from_total_energy` on compressive non-pure cells.
- `compressive_recovery` (`4024-4032`): M7 solve then
  `_recover_pressure_from_total_energy` on `_compressive_pressure_mask & ~pure`.
- `implicit_energy`/`apec_pe` (`4000-4015`): use `_solve_acoustic_energy_ad`
  (`3684-...`) instead — NOT on the 02A/07B path; lower priority.
C++: enum `PressureClosure`; port `pressure_work_consistent` + `compressive_recovery`
first, `implicit_energy` later.

### M9 — Newton primitive recovery — REUSE
Already ported: `primitive.hpp::cons_to_prim_W` (Newton on p,T1,T2). The
`imex_ad_step` tail (`4076-4081`) reconstructs W from (q1,q2,α,p) directly by
`rho_k=q_k/α_k`, `e_k=eos.energy`, `T_k=eos.temperature` — this simpler explicit
recovery (NOT the full 3×3 Newton) is what runs once p_new is known; port it as a
small `prim_from_alpha_p()` helper. Full Newton `cons_to_prim_W` is reused where
p is unknown.

### M10 — Step driver + config
**`imex_ad.py:3902-4090`**. Order: normalise closure → regime_auto (M8) →
single-phase shortcuts (`3929-3953`: if whole domain pure → `_single_phase_euler_rusanov_step`)
→ `_material_update` (M6) → clip α to [1e-12, 1-1e-12] → acoustic solve (M7) +
closure (M8) → near-vacuum velocity regularisation (`_regularize_near_vacuum_velocity`
272-318) → primitive LMP/LED filter (`_primitive_lmp_effective_mode` 359-376,
default off unless env) → recover W (M9). `main.solve` (`main.py:330-509`) is the
time loop: dt from acoustic CFL (`_max_acoustic_dt` 35-...) or `dt_fixed`,
optional source-term half-steps (`394-401`, inactive for 02A/07B), inlet
resolution, then dispatch to `imex_ad_step`. C++: `step.hpp::imex_ad_step()` +
`solver.hpp::solve_imex_ad()` provide the production stage and thin driver loop.
The latter covers fixed/adaptive acoustic CFL time steps, final-time truncation,
max-step termination, non-finite guards, and optional per-step metadata.
`imex_ssp3` supports the Python default transport-acoustic CN scope, full-step
Shu-Osher composition, and the Pareschi-Russo stage-residual form.

`apps/five_eq_02a_validate.cpp` drives the completed C++ production loop over
the full 100-step periodic NASG pressure-equilibrium case and applies the core
acceptance metrics: pressure/velocity preservation, admissibility, contact
range, correlation, and normalized L1 error.  The Python-only oscillation plot
and high-frequency guard remain validation-harness work, not solver work.

`apps/five_eq_07b_validate.cpp` ports the three 07-B initial states and the
linear acoustic d'Alembert reference used for their core profile metrics.  It
reports the common L2/Linf, pointwise-fraction, L1, correlation, absolute
peak-position, packet-amplitude, and wave-symmetry gates for Air-Water,
Helium-Air, and Argon-Air.  It also applies the signed-extrema,
checkerboard/interface-residual, high-frequency, local-TV, sharp-overshoot,
and Air-Water wiggle guards through
`cfd/validation/oscillation_guards.hpp`.  Plot rendering remains Python-only;
the C++ executable is a numerical gate.

---

## Explicitly NOT ported

- **torch autodiff** (`imex_ad.py:23-24, 2917-3553`) → replaced by the
  hand-derived analytic banded Jacobian (M7). This is the whole point of the port.
- **`autograd.jacobian`** scalar n<32 branch (`3629-3681`) → same analytic
  tridiagonal assembly; keep only as a cross-check.
- Python environment-variable plumbing itself.  C++ exposes the covered choices
  as typed configuration fields rather than process-global environment state.
- A production large-grid implicit ND pressure block; existing ND coverage is
  explicit HLLC/SSPRK3 field evolution, not the 1-D IMEX acoustic block.

## EOS / primitive reuse — confirmed
- `cfd/eos.hpp` (`EOS::Ideal/SG/NASG`) provides ALL four (p,T) derivatives M1
  needs: `drhodp_T` (`eos.hpp:121`), `drhodT_p` (133), `dedp_T` (145),
  `dedT_p` (161), plus `density`(108), `energy`(60), `temperature`(72),
  `pressure_from_rhoT`(96). **No gap** for the 5eq sound-speed / face-EOS needs.
- `cfd/primitive.hpp` provides `PrimW{alpha1,T1,T2,u,p}`, `ConsU{m1,m2,mom,rhoE,a1}`,
  `prim_to_cons_W`, `cons_to_prim_W` (Newton on p,T1,T2), `dUdW_analytic`,
  `EOS::max2`. Covers M9 + the extend/ghost EOS state builds. **No gap.**

## Oracle files (`cpp/tests/5eq_ref/`, from `cpp/tools/gen_5eq_oracle.py`)
| File | Module | Rows | What |
|------|--------|------|------|
| `sound_speed_ref.txt` | M1 | 13 | c_mix² + Z sweep over α incl. pure branch (air/NASG-water) |
| `weno5_face_ref.txt` | M7 | 9 | WENO5-JS on smooth/near-flat/flat/monotone/extremum stencils |
| `alpha_bvd_ref.txt` | M4 | 26 | adaptive_bvd: pure-jump (CICSAM) + smooth (MUSCL-Hancock) |
| `slau2_faces_ref.txt` | M3 | 9 | SLAU2 faces, 8-cell air|water jump + bump |
| `acoustic_solve_ref.txt` | M7 | 40 | `_solve_acoustic_ad` (u_new,p_new), weno5 active |
| `step_02A_ref.txt` | M6/8/10 | 100 | full W after 1 production step, 02_A IC |
| `step_07B_ref.txt` | M6/8/10 | 400 | full W after 1 production step, 07_B Air-Water IC |
| `run_02A_ref.txt` | M10 | 100 | full W after three fixed-dt production steps, 02_A IC |
| `run_07B_ref.txt` | M10 | 400 | full W after three CFL production steps, 07_B Air-Water IC |

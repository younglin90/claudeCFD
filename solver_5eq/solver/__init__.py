# solver_5eq/solver — five-equation model solver package (relocated 2026-07-02).
# Intentionally empty: sub-packages (five_eq_IMEX, five_eq_IMEX_v2, He2024) are
# imported directly; drivers/tests insert solver_5eq/ into sys.path so
# `solver.five_eq_IMEX...` resolves here without executing legacy imports.
